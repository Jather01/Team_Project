<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cart">

	<!-- 장바구니 목록 보기 -->
	<select id="listCart" resultType="cartDto">
		select cart_id, p.product_id, c.userid, name, product_name, amount, price,
		(price*amount) money

		where m.userid=c.userid and c.product_id=p.product_id and
		c.userid=#{userid} order by cart_id
	</select>

	<!-- 장바구니에 추가하기 -->
	<insert id="insert">
		insert into cart (cart_id, userid, product_id, amount)
		values
		(seq_cart.nextval, #{userid}, #{product_id}, #{amount} )
	</insert>

	<!-- 장바구니 합계 -->
	<select id="sumMoney" resultType="int">

		select nvl(sum(price*amount),0)
		from cart c,product p
		where c.product_id=p.product_id and userid=#{userid}

	</select>
	
	<!-- 장바구니 개별 상품 삭제 -->
    <delete id="delete">
        delete from cart where cart_id=#{cart_id}
    </delete>
    
    <!-- 장바구니 비우기 -->
    <delete id="deleteAll">
        delete from cart where userid=#{userid}
        <!-- 장바구니테이블로부터 회원의 아이디와 삭제하고자하는 회원의 아이디가 같으면 장바구니에 있는 자료를 전부다 삭제 -->
    </delete>
    
</mapper>